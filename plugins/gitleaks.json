{
  "name": "gitleaks",
  "version": "1.0.0",
  "tools": [
    {
      "name": "ctx_gitleaks_detect",
      "description": "Detect secrets and sensitive data in the Git repository history and working tree.",
      "command": "gitleaks detect --source . --verbose",
      "timeout_ms": 60000
    },
    {
      "name": "ctx_gitleaks_protect",
      "description": "Scan only staged changes for secrets before committing.",
      "command": "gitleaks protect --staged --verbose",
      "timeout_ms": 60000
    }
  ]
}
